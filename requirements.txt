# Top-level requirements for all odmlib_examples programs.
# Each example directory also has its own requirements.txt; this file
# aggregates every third-party dependency so the whole repo can be set up
# from a single virtual environment at the repo root.

# Core: odmlib object model for ODM / Define-XML
#   The 0.2.0rc1 pin installs the release candidate from PyPI today and will
#   automatically prefer the final 0.2.0 once it is published.
odmlib>=0.2.0rc1

# XML Schema validation of ODM / Define-XML documents
xmlschema>=1.10.0

# Excel I/O
#   read:  xlsx2define2-1
#   write: define2-1-to-xlsx, snippets
openpyxl>=3.0.9
XlsxWriter>=3.0.3

# CDISC Library REST API client (library_xml)
requests>=2.26.0

# Tabular data handling (snippets and define builders)
pandas>=1.3.0

# Run the Jupyter notebook examples (notebooks/)
jupyter>=1.0.0
notebook>=6.4.0
