# Top-level requirements for all odmlib_examples programs.
# Each example directory also has its own requirements.txt; this file
# aggregates every third-party dependency so the whole repo can be set up
# from a single virtual environment at the repo root.

# Core: odmlib object model for ODM / Define-XML
odmlib>=0.2.0

# XML Schema validation of ODM / Define-XML documents
xmlschema>=1.10.0

# Excel I/O
#   read:  xlsx2define2-1, extended-define2xlsx, gendefine, xls2define
#   write: define2-1-to-xlsx, define2xls, v0-2-snippets
openpyxl>=3.0.9
XlsxWriter>=3.0.3

# CDISC Library REST API client (library_xml)
requests>=2.26.0

# JSON Schema conformance checks (gendefine)
jsonschema>=4.0.0

# Tabular data handling (v0-2-snippets and define builders)
pandas>=1.3.0

# Run the Jupyter notebook examples (notebooks/, v0-2-notebooks/)
jupyter>=1.0.0
notebook>=6.4.0
